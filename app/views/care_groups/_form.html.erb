<div class="container">
<%= form_for(@care_group) do |f| %>
  <% if @care_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@care_group.errors.count, "error") %> prohibited this care_group from being saved:</h2>

      <ul>
      <% @care_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <form class="form-horizontal " role="form">
    <div class="jumbotron p-and-cg">
    <fieldset>
      <legend class="text-center"> <%= yield (:title)%></legend> 
      <div class="form-group">
        <%= f.label :name, :class => 'col-lg-2 control-label' %>
        <div class="col-lg-10">
          <%= f.text_field :name, :class =>'input form-control'  %>
        </div>
      </div>
      <div class="form-group">
        <div class="col-lg-10 col-lg-offset-2">
          <%= f.submit :class => 'btn btn-small btn-primary' %>
        </div>
      </div>
    </fieldset>
    </div>
  </form>

<% end %>
</div>
