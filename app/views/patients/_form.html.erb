<%= form_for(@patient) do |f| %>
  <% if @patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient.errors.count, "error") %> prohibited this patient from being saved:</h2>
      <ul>
        <% @patient.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
 
      <div class="form-group">
        <%= f.label :name, :class => 'col-lg-2 control-label' %>
        <div class="col-lg-10">
          <%= f.text_field :name, :class =>'input form-control' %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :email, :class => 'col-lg-2 control-label'  %>
        <div class="col-lg-10">
          <%= f.text_field :email, :class => 'input form-control' %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :phone, :class => 'col-lg-2 control-label'  %>
        <div class="col-lg-10">
          <%= f.text_field :phone, :class => 'input form-control' %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :condition, :class => 'col-lg-2 control-label'  %>
        <div class="col-lg-10">
          <%= f.text_area :condition, :class => 'input form-control' %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :deviceId, :class => 'col-lg-2 control-label'  %>
        <div class="col-lg-10">
          <%= f.number_field :deviceId, :class => 'input form-control' %>
        </div>
      </div>
      <div class="form-group">
        <div class="col-lg-10 col-lg-offset-2">
          <%= f.submit "Submit", :class => 'btn btn-small btn-primary' %>
        </div>
      </div>

<% end %>

